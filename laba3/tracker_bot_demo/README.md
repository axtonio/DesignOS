# Tracker

Сохранил код, потому что использую переменные окружающей среды в папке [configs/production](./configs/production/) и синькаю их с помощью volume. Все конечно можно закинуть в docker compose, но там их много и мне лично легче так контролировать свою среду

## StartUp

```sh
docker compose up -d
```
